Basic:
  type: http
  scheme: basic

ApiKey:
  type: apiKey
  in: header
  name: X-Redmine-API-Key

ApiKeyInQuery:
  type: apiKey
  in: query
  name: key

OAuth2:
  type: oauth2
  flows:
    authorizationCode:
      authorizationUrl: /oauth/authorize
      tokenUrl: /oauth/token
      scopes: {}
