oneOf:
  - type: object
    required:
      - id
      - project
      - user
      - roles
    properties:
      id:
        type: integer
      project:
        $ref: ./id_name.yml
      user:
        $ref: ./id_name.yml
      roles:
        type: array
        items:
          $ref: ./id_name.yml
  - type: object
    required:
      - id
      - project
      - group
      - roles
    properties:
      id:
        type: integer
      project:
        $ref: ./id_name.yml
      group:
        $ref: ./id_name.yml
      roles:
        type: array
        items:
          $ref: ./id_name.yml
