type: array
items:
  type: object
  required:
    - id
    # - value
  properties:
    id:
      type: number
  # Workaround: Some generators do not support "oneOf".
  additionalProperties:
    description: "value: string? | string[]"
  example:
    id: 0
    value: "string"
