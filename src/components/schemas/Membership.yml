oneOf:
  - type: object
    required:
      - id
      - project
      - user
      - roles
    properties:
      id:
        type: integer
      project:
        $ref: ./IdName.yml
      user:
        $ref: ./IdName.yml
      roles:
        type: array
        items:
          $ref: ./IdName.yml
  - type: object
    required:
      - id
      - project
      - group
      - roles
    properties:
      id:
        type: integer
      project:
        $ref: ./IdName.yml
      group:
        $ref: ./IdName.yml
      roles:
        type: array
        items:
          $ref: ./IdName.yml