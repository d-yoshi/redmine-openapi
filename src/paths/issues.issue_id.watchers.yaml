post:
  tags:
    - Issues
  externalDocs:
    url: https://www.redmine.org/projects/redmine/wiki/Rest_Issues#Adding-a-watcher
  summary: Add watcher
  description: Returns 204 even when the user_id does not exist (the watcher is silently not added). A nonexistent issue yields 403, not 404.
  operationId: addWatcher
  parameters:
    - $ref: ../components/parameters/path/format.yaml
    - $ref: ../components/parameters/path/issue_id.yaml
    - $ref: ../components/parameters/header/x_redmine_switch_user.yaml
  requestBody:
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          required:
            - user_id
          properties:
            user_id:
              type: integer
  responses:
    204:
      description: ""
