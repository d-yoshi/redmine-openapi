type: object
properties:
  issues:
    type: array
    items:
      allOf:
        - $ref: "./Issue.yml"
        - $ref: "./Issue.partial.yml"
  total_count:
    type: integer
  offset:
    type: integer
  limit:
    type: integer
required:
  - issues
  - total_count
  - offset
  - limit
